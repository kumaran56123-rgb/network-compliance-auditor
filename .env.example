# REST backend base URL. The api/ layer reads this at runtime.
# Example: http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

# Set to "mock" to use the in-browser stub instead of calling the backend.
# Useful for frontend-only development before backend/ML is ready.
VITE_USE_MOCK=true
